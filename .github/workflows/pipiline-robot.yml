name: Pipeline Test

on: push

jobs:
  test_api:
    name: API Tests - Robot
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório - API
        uses: actions/checkout@v4

      - name: Instalação e Configuração do Python - API
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
    
      - name: Instalar Dependências do Projeto - API
        run: |
          pip install robotframework
          pip install robotframework-requests
          pip install robotframework-faker
          pip install robotframework-jsonlibrary
          pip install setuptools

      - name: Execução dos Testes de API com Robot
        run: |
          mkdir -p results/api/
          robot --name "Test Suite" --outputdir results/api/ --output output_api.xml tests/api/

      - name: Resultados dos Testes - API
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: resultado-testes-api
          path: results/api/

  test_web:
    name: Web Tests - Robot
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório - WEB
        uses: actions/checkout@v4

      - name: Instalação e Configuração do Python - WEB
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar Dependências do Projeto - WEB
        run: |
          pip install robotframework-seleniumlibrary

      - name: Execução dos Testes WEB com Robot
        run: |
          mkdir -p results/web/
          robot --name "Test Suite" --outputdir results/web/ --output output_web.xml tests/web/
  
      - name: Resultados dos Testes - WEB
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: resultado-testes-web
          path: results/web/

  merge:
    name: Merge Reports
    runs-on: ubuntu-latest
    needs: 
      - test_api
      - test_web

    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install robotframework

      - name: Download Artifacts - API
        uses: actions/download-artifact@v4
        with:
          name: resultado-testes-api
          path: results/api/

      - name: Download Artifacts - WEB
        uses: actions/download-artifact@v4
        with:
          name: resultado-testes-web
          path: results/web/

      - name: List Files in Results Directory
        run: |
          echo "Verificando arquivos no diretório de resultados (API e WEB):"
          ls -R results/

      - name: Merge Reports
        run: |
          rebot --merge results/api/output_api.xml results/web/output_web.xml --output merged_output.xml
          ls -a

      - name: Upload Merge Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: robot-framework-report
          path: merged_output.xml

  deploy:
    name: Deploy GitHub Pages 🌐
    runs-on: ubuntu-latest
    needs: 
      - merge
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: https://jvluzc.github.io/RobotTestsAPI/report.html
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with: 
          name: robot-framework-report

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./merged_output.xml

      - name: Deploy to Github Pages
        id: deployment
        uses: actions/deploy-pages@v4
